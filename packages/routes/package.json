{
    "name": "@owenii/routes",
    "version": "0.0.0",
    "exports": {
        "./api": {
            "require": "./api.js",
            "types": "./api.d.ts"
        }
    },
    "scripts": {
        "lint": "eslint --ext .js,.ts",
        "format": "prettier --write \"**/*.{js,ts}\" && npm run lint -- --fix",
        "build": "tsc"
    },
    "devDependencies": {
        "tsup": "^6.5.0",
        "typescript": "^4.9.3"
    },
    "nx": {
        "targets": {
            "format": {
                "dependsOn": ["^format"],
                "outputs": ["{projectRoot}/*"]
            },
            "build": {
                "dependsOn": ["^build"],
                "outputs": ["{projectRoot}/*"]
            }
        }
    }
}
